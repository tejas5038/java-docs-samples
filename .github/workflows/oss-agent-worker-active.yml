name: OSS Agent Worker Active

on:
  workflow_dispatch:
  push:
    branches: [main]
  schedule:
    - cron: '*/5 * * * *'

permissions:
  contents: write
  pull-requests: write
  id-token: write

jobs:
  worker:
    uses: tejas5038/osv-scanner/.github/workflows/oss-agent-worker-reusable.yml@main
